# packages/db/.env (template — used by migrator + seeder; NO real secrets)
# Point at the dedicated Neon project mvs-compliance-comp (app RW role).
DATABASE_URL=postgresql://__SET_IN_INFISICAL__@__NEON_HOST__/comp?sslmode=require
